import { test } from '@playwright/test';

test.skip('Playwright Electron runner is disabled in this stack; use tests/e2e/system/smoke.electron.js instead', async () => {
  // Intentionally skipped.
});
